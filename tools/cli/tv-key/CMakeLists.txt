SET(INSTALL_DIR
  "/opt/usr/share/tizen-tools/cli/tv-key")

INSTALL(PROGRAMS "${CMAKE_CURRENT_SOURCE_DIR}/tv-key"
  DESTINATION ${INSTALL_DIR})
INSTALL(FILES "${CMAKE_CURRENT_SOURCE_DIR}/tool.md"
  DESTINATION ${INSTALL_DIR})
